// import getLectureInfo from "../getLectureInfo";

const addLecture = () => {
  // const addLecture = (userName, lectureCode, lectureName, lectureTime) => {
  // const lectureInfo = getLectureInfo();
  // const lectureInfo = getLectureInfo(userName);
  return 1;
};

export default addLecture;
